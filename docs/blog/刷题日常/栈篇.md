---
title: 栈篇
createTime: 2026/04/28 15:34:58
permalink: /blog/wh2z8b2m/
---
###   2026-4-28 leeCode-20
::: info 题目详情

[20. 有效的括号](https://leetcode.cn/problems/valid-parentheses/)

给定一个只包括 `'('`，`')'`，`'{'`，`'}'`，`'['`，`']'` 的字符串 `s` ，判断字符串是否有效。

有效字符串需满足：

1. 左括号必须用相同类型的右括号闭合。
2. 左括号必须以正确的顺序闭合。
3. 每个右括号都有一个对应的相同类型的左括号。

**示例 1：**

输入：s = "()"

输出：true

**示例 2：**

输入：s = "()[]{}"

输出：true

**示例 3：**

输入：s = "(]"

输出：false

**示例 4：**

输入：s = "([])"

输出：true

**示例 5：**

输入：s = "([)]"

输出：false

 ```

**提示：**

- 1 <= s.length <= 10^4^
- `s` 仅由括号 `'()[]{}'` 组成

:::

::: details 个人思路

定义一个栈 `stack`  遍历数组

- 当遇到`{`  `[` `(` 时直接压入`stack`  中

- 当遇到 `]` `}` `)`时 获取`stack`  的栈顶元素 是否是其对应的括号`{`  `[` `(`，是的话弹出栈顶元素，不是直接return false
- 最后判断 `stack`  是否为空 

时间复杂度	O(N)

```java
class Solution {
	public boolean isValid(String s) {
		ArrayDeque<String> stack = new ArrayDeque<>();
		// ArrayDeque<String> rStack = new ArrayDeque<>();
		String[] split = s.split("");
		if (s.startsWith("]")||s.startsWith(")")||s.startsWith("}")){
			return false;
		}
		for(String str : split) {
			if (str.charAt(0) == '['||str.charAt(0) == '{'||str.charAt(0) == '(') {
				stack.push(str);
			}else if(str.charAt(0) == ']'){
				if("[".equals(stack.peek())){
					stack.pop();
				}else{
					return false;
				}
			}else if(str.charAt(0) == '}'){
				if("{".equals(stack.peek())){
					stack.pop();
				}else{
					return false;
				}
			}else if(str.charAt(0) == ')'){
				if("(".equals(stack.peek())){
					stack.pop();
				}else{
					return false;
				}
			}
		}
		// rereturn stack.isEmpty()&&rStack.isEmpty();
        return stack.isEmpty();
	}
}
```

::: 

::: details 官方思路

:::tabs

@tab 栈

我们遍历给定的字符串 s。当我们遇到一个左括号时，我们会期望在后续的遍历中，有一个相同类型的右括号将其闭合。由于后遇到的左括号要先闭合，因此我们可以将这个左括号放入栈顶。

当我们遇到一个右括号时，我们需要将一个相同类型的左括号闭合。此时，我们可以取出栈顶的左括号并判断它们是否是相同类型的括号。如果不是相同的类型，或者栈中并没有左括号，那么字符串 s 无效，返回 False。为了快速判断括号的类型，我们可以使用哈希表存储每一种括号。哈希表的键为右括号，值为相同类型的左括号。

在遍历结束后，如果栈中没有左括号，说明我们将字符串 s 中的所有左括号闭合，返回 True，否则返回 False。

注意到有效字符串的长度一定为偶数，因此如果字符串的长度为奇数，我们可以直接返回 False，省去后续的遍历判断过程。



```
class Solution {
    public boolean isValid(String s) {
        int n = s.length();
        if (n % 2 == 1) {
            return false;
        }

        Map<Character, Character> pairs = new HashMap<Character, Character>() {{
            put(')', '(');
            put(']', '[');
            put('}', '{');
        }};
        Deque<Character> stack = new LinkedList<Character>();
        for (int i = 0; i < n; i++) {
            char ch = s.charAt(i);
            if (pairs.containsKey(ch)) {
                if (stack.isEmpty() || stack.peek() != pairs.get(ch)) {
                    return false;
                }
                stack.pop();
            } else {
                stack.push(ch);
            }
        }
        return stack.isEmpty();
    }
}
```

@tab 官方 0ms 

最天才的一集，不Debug我都不知道他在干什么 ┭┮﹏┭┮

以（{[]}) 为例

首先遍历char[] ch 将遇到的所有`(` `{` `[`  改为 `]` `}` `)`，并将index 加一  此时 原 ch 已被修改为  `)` `}` `]` `]` `}` `)`

当遇到  `]` `}` `)` 时会判断 前一个元素是否与自己相等  并将index -1 

直到index = 0 或者 ch[--index]!=c 

也就是将原数组作为栈 当遍历到 `(` `{` `[` 时将对应的  `]` `}` `)` 压入栈中 ，当遇到 `]` `}` `)`时获取栈顶元素比较

直到栈空或者元素不匹配时返回

```java
class Solution {
    public boolean isValid(String s) {
        if(s.length()%2!=0){
            return false;
        }
        char[] ch = s.toCharArray();
        //栈顶指针
        int index = 0; // [!code warning]
        for(char c : ch){
            if(c=='('){
                //将)入栈 栈定指针加一
                ch[index++]=')'; // [!code warning]
            }else if(c=='['){
                ch[index++]=']';
            }else if(c=='{'){
                ch[index++]='}';
                //index==0 表示栈已经空了 但是当前元素为 ],},) 所以没有匹配他的括号了
                //ch[--index]!=c 代表栈顶元素与当前元素不匹配 ，如果匹配栈顶指针也向下移动一位，代表出栈操作
            }else if(index==0||ch[--index]!=c){ // [!code warning]
                return false;
            }
        }
        return index==0;
    }
}
```

